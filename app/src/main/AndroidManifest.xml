<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allen.recyclerview">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RecyclerClickItemActivity" />
        <activity android:name=".activity.VerticalAdjustActivity" />
        <activity android:name=".activity.HorizontalAdjustActivity" />
        <activity android:name=".activity.ListActivity" />
        <activity android:name=".activity.CustomView" />
        <activity android:name=".activity.GooglePlayActivity" />
        <activity android:name=".activity.HideActivity" />
        <activity
            android:name=".activity.OptimizeItemClickActivity"
            android:label="@string/title_activity_optimize_item_click"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.OptimizeItem2ClickActivity"
            android:label="@string/title_activity_optimize_item_click"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.LocalRefreshActivity"></activity>
    </application>

</manifest>
